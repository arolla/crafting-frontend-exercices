import { BetChoice } from '../../models'
import Any = jasmine.Any

export function updateBetsSlip(betsSlip: Any[], gameOdds: Any, betChoice: BetChoice): Any[] {
    // not yet implemented
    //don't worry about that for now
    //it will be responsible for updating the betsSlip array with the new bet
    return []
}
