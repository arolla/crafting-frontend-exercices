import type { BetChoice } from '../../models/bets/betChoice'

export const updateBetsSlip = (
  _betsSlip: unknown[],
  _gameOdds: unknown,
  _betChoice: BetChoice,
): unknown[] => {
  // Not yet implemented
  // don't worry about that for now
  // it will be responsible for updating the betsSlip array with the new bet
  return []
}
